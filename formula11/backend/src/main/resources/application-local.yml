spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/formula11
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
app:
  jwt:
    secret: change-me-local-secret-key-must-be-at-least-32-bytes
